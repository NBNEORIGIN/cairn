version: '3.8'

services:
  deek-web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DEEK_API_URL: ${DEEK_API_URL:-http://deek-api:8765}
      DEEK_API_KEY: ${DEEK_API_KEY}
      PHLOE_API_URL: ${PHLOE_API_URL:-https://phloe.co.uk}
      PHLOE_TENANT_SLUG: ${PHLOE_TENANT_SLUG:-nbne}
    ports:
      - "127.0.0.1:3020:3000"
    networks:
      - cairn

networks:
  cairn:
    external: true
